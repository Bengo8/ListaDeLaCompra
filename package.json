{
  "name": "lista-de-la-compra",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "16.20.2",
    "tsc": "5.2.2"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^4.3.1",
    "react-scripts": "4.0.3",
    "serve": "^14.2.1",
    "web-vitals": "^1.1.2"
  },
  "scripts": {
    "start": "serve build -s -n -L -p $PORT",
    "startOld": "react-scripts start  --openssl-legacy-provider start",
    "startDev": "npm run dev",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
